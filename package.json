{
  "name": "scriba-news-aggregator",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "prisma:generate": "bunx prisma generate",
    "prisma:push": "bunx prisma db push",
    "bun:build": "bun build ./index.ts --outdir ./out"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0"
  }
}